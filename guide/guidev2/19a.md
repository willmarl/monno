# part 19a | admin columns — no image column

> **PATH: none only.** Omit the image/media column entirely. No extra imports needed.

Insert nothing between the title column and content column in `19.md` step 1.

<!-- NEXT: 22-admin-create.md (skip if !ADMIN) then 20.md (skip if no search) else 21.md per ROUTER -->
