# part 8 | Test CRUD endpoints/summary

## step 1 | Generate Bruno collection file (optional archive)

Generate a Bruno collection JSON file so the human can import it later if they want. This is **optional backup** — the live smoke test in step 2 is the gate.

Save the file as `guide/{{resource}}-bru.json`.

Use `guide/article-resource-bru.json` as the format reference — the structure, field names, and variable conventions must match exactly.

**Rules:**

- Use `{{base_URL}}` for the host (never hardcode `http://localhost:3000`)
- Use `{{resourceId}}` (e.g. `{{blogId}}`) and `{{userId}}` for path variables — match Bruno variable naming from article example
- Only include requests for endpoints that were actually implemented based on the PROJECT-BRIEF
- Skip admin folder entirely if admin was not requested
- Skip cursor requests if cursor pagination was not implemented
- Skip search request if search was not implemented
- For `multipartForm` bodies: include only the fields that exist on the actual schema
- `seq` values should be sequential integers starting at 1 per folder
- Set `auth.mode` to `"inherit"` on all requests and folders

**Folder structure to generate (adapt based on what was implemented):**

```
root items:
  - folder: "admin" (skip if no admin)
      - folder: "{{resource}}" (lowercase)
          - requests: get all, get all cursor, get by id, search, update, delete, restore
          - create (if admin create was implemented — see 3-admin-create.md)
  - folder: "{{resources}}" (plural lowercase)
      - requests: create, get all, get all cursor, get by user, get by user cursor,
                  get by id, search, search suggest, update, delete,
                  get user's liked (if likes implemented),
                  collections containing this (if collections implemented)
```

After generating the file, tell the human:

> "Bruno collection saved to `guide/{{resource}}-bru.json` (optional). Primary gate is `pnpm run ai-api-test` below."

---

## step 2 | Live smoke test via `pnpm run ai-api-test` (REQUIRED GATE)

**Do NOT continue to Part 9+ / frontend until this passes.**

1. Tell the human: ensure API is running (`pnpm dev`), then run:

```bash
pnpm run ai-api-test
```

2. Human pastes the script's prompt into **this same chat**.
3. You: plan a short smoke suite from the endpoints below (only what was implemented), then **run curl commands** using the provided admin `Authorization: Bearer …` and `sessionId` cookie.
4. Fix any failures, re-test, then ask human to confirm before continuing.

**Smoke plan should cover (skip N/A):**

**Create**
POST `/{{resource}}`
ex: POST `/articles`

**Read all**
GET `/{{resource}}`
cursor: `/{{resource}}/cursor` (if cursor)

**Get by user**
GET `/{{resource}}/users/<userId>`
cursor variant if implemented

**Read single**
GET `/{{resource}}/<id>`

**Update**
PATCH `/{{resource}}/<id>`

**Delete**
DELETE `/{{resource}}/<id>`

### admin (if ADMIN)

**Create (admin)** — if `3-admin-create.md` was done
POST `/admin/{{resource}}`

**Read all / by id / update / delete / restore**
GET/PATCH/DELETE `/admin/{{resource}}` …
POST `/admin/{{resource}}/<id>/restore`

### complex media (if PATH_LETTER=c) — see also `8c.md`

Media sub-routes on user + admin after create.

---

## Endpoint cheat sheet (for your smoke plan)

**Create**
POST `http://localhost:<PORT>/{{resource}}`
ex: POST `http://localhost:3001/articles`

```json
{
  "title": "First Article",
  "content": "Hello world!",
  "status": "DRAFT"
}
```

**Read all**
GET `http://localhost:<PORT>/{{resource}}`
cursor: `…/{{resource}}/cursor`

**Get all user's {{resource}}**
GET `…/{{resource}}/users/<userId>`

**Read single**
GET `…/{{resource}}/<id>`

**Update**
PATCH `…/{{resource}}/<id>`

**Delete**
DELETE `…/{{resource}}/<id>`

## admin endpoints/summary

**Create (admin)**
POST `…/admin/{{resource}}`

```json
{
  "title": "Admin-created title",
  "content": "Body text",
  "status": "DRAFT"
}
```

**Read all**
GET `…/admin/{{resource}}`
cursor: `…/admin/{{resource}}/cursor`

**Read single**
GET `…/admin/{{resource}}/<id>`

**Update**
PATCH `…/admin/{{resource}}/<id>`

**Delete**
DELETE `…/admin/{{resource}}/<id>`

**Restore**
POST `…/admin/{{resource}}/<id>/restore`

<!-- NEXT: 8c.md if PATH_LETTER=c, else 9.md (skip 9 if no search) -->
