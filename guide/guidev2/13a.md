# part 13a | frontend api — no file upload

> **PATH: none only.** No media fields, no upload API/hook changes.

Apply these deltas after completing the matching steps in `13.md`.

## step 1 — types (no media fields)

No media fields on `{{resource}}`. The shared interface in `13.md` is complete as-is.

## step 2 — admin types (no media fields)

No media fields on admin `{{resource}}`. The shared interface in `13.md` is complete as-is.

## step 4 — api.ts

No changes. Create and update stay JSON-only.

## step 6 — admin api.ts

No changes.

## step 8 — hooks.ts

No changes. `useCreate{{resource}}` and `useUpdate{{resource}}` stay as in `13.md`.

## step 10 — admin hooks.ts

No changes. `useAdminUpdate{{resource}}` stays as in `13.md` step 9.

<!-- NEXT: 14.md then 14{L}.md -->
