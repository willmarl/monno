# part 16 | make generic component for {{resource}}

Open **`16{L}.md` only** (replace `{L}` with your PATH letter from ROUTER).

Do not read other `16*.md` files — each contains the full component for one media path.

<!-- NEXT: 16{L}.md -->
